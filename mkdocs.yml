site_name: Abismal_torch
docs_dir: docs
nav:
  - Home: index.md
  - API: api.md
theme: readthedocs

plugins:
  - search
  - autorefs
  - mkdocstrings:
      default_handler: python
      handlers:
        python:
          paths: [abismal_torch]
          options:
            docstring_style: google
            annotations_path: full
